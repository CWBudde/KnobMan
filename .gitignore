# Go
/go.sum

# WASM build artifact (regenerated by `make build-wasm`)
web/knobman.wasm

# wasm_exec.js committed so repo is self-contained without a Go toolchain
# web/wasm_exec.js

# OS
.DS_Store
Thumbs.db

#AI agents
.claude/